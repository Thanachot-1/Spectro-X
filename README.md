# DurianScan AI 🍈

Web application สำหรับวัดความสุกทุเรียนด้วย AI วิเคราะห์จากรูปภาพ โดยใช้ Google Gemini API

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

- [Node.js](https://nodejs.org/) (เวอร์ชัน 18 หรือใหม่กว่า)
- Google Gemini API Key (ขอได้ที่ [Google AI Studio](https://aistudio.google.com/))

## 🚀 วิธีการรันใน VS Code (Getting Started)

1. **ติดตั้ง Dependencies**
   เปิด Terminal ใน VS Code และรันคำสั่ง:
   ```bash
   npm install
   ```

2. **ตั้งค่า API Key**
   สร้างไฟล์ชื่อ `.env` ที่ root ของโปรเจกต์ (ระดับเดียวกับ package.json) และใส่ API Key ของคุณลงไป:
   ```env
   API_KEY=ใส่_GEMINI_API_KEY_ของคุณที่นี่
   ```

3. **รันโปรเจกต์**
   รันคำสั่งเพื่อเริ่มเซิร์ฟเวอร์:
   ```bash
   npm run dev
   ```

4. **ใช้งาน**
   กดลิงก์ที่ปรากฏใน Terminal (ปกติคือ `http://localhost:5173`) เพื่อเปิดใช้งานบน Browser

## 🛠️ เทคโนโลยีที่ใช้

- **Frontend**: React, TypeScript, Tailwind CSS
- **Charts**: Recharts
- **Icons**: Lucide React
- **AI Model**: Google Gemini 2.5 Flash
- **Build Tool**: Vite

## ⚠️ หมายเหตุ

- โปรเจกต์นี้ใช้ `gemini-2.5-flash` model ซึ่งมีความเร็วสูงและแม่นยำในการวิเคราะห์ภาพ
- การวิเคราะห์ขึ้นอยู่กับคุณภาพของรูปภาพและแสงไฟ
